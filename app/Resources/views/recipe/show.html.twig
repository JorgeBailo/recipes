{% extends 'base.html.twig' %}

{% block body %}
    <h1>Recipe</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recipe.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ recipe.title }}</td>
            </tr>
            <tr>
                <th>Body</th>
                <td>{{ recipe.body }}</td>
            </tr>
        </tbody>
    </table>
    {{ form_start(delete_form) }}
        <input type="submit" class="waves-effect waves-light btn red" value="Delete">
    {{ form_end(delete_form) }}

    <br />

    <a href="{{ path('recipe_index') }}" class="waves-effect waves-light btn blue">Back to the list</a>
    <a href="{{ path('recipe_edit', { 'id': recipe.id }) }}" class="waves-effect waves-light btn green">Edit</a>

    </ul>
{% endblock %}
